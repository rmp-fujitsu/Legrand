// if [tab] is selected
if (typeof ready !== 'undefined') {
if (ready) {
		var selected_tab = "chart";
		id_selected_dashboard_tab.setValue(selected_tab);
		// console.log('onglet Graphes sélectionné');
		// getFilter();
}}
